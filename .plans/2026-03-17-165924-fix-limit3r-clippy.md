# Fix all clippy errors in the limit3r package

**Date:** 2026-03-17 16:59
**Task:** Fix 37 clippy errors from guardrail3 tightening rules

## Goal
Zero clippy errors on `cargo clippy -p limit3r --all-targets`.

## Approach

### Errors by category

1. **`needless_raw_string_hashes`** (1 error) — `option_tests.rs:39` — remove `#` from `r#"{}"#`
2. **`disallowed_methods: serde_json::from_str`** (21 errors) — all in test files — add module-level `#[allow(clippy::disallowed_methods)]` with reason
3. **`float_cmp`** (2 errors) — `config/tests.rs:31,57` — add `#[allow(clippy::float_cmp)]` on affected test fns
4. **`approx_constant`** (1 error) — `non_option_guard_tests.rs:32` — add `#[allow(clippy::approx_constant)]` on test fn
5. **`missing_backticks`** (1 error) — `duration_serde/tests.rs:6` — add backticks around `duration_serde`
6. **`significant_drop_tightening`** (11 errors) — circuit_breaker/tests.rs (7), rate_limiter/tests.rs (3), bulkhead/tests.rs (1) — add module-level `#[allow(clippy::significant_drop_tightening)]` with reason

### Strategy
- For `disallowed_methods` and `significant_drop_tightening`: add module-level allows since nearly every test in the file uses these patterns
- For `float_cmp` and `approx_constant`: add function-level allows
- For `needless_raw_string_hashes` and `missing_backticks`: direct code fixes

## Files to Modify
- `packages/limit3r/src/config/tests.rs` — add disallowed_methods + float_cmp allows
- `packages/limit3r/src/duration_serde/option_tests.rs` — add disallowed_methods, fix raw string
- `packages/limit3r/src/duration_serde/tests.rs` — add disallowed_methods, fix backticks
- `packages/limit3r/src/duration_serde/non_option_guard_tests.rs` — add disallowed_methods + approx_constant
- `packages/limit3r/src/circuit_breaker/tests.rs` — add significant_drop_tightening
- `packages/limit3r/src/rate_limiter/tests.rs` — add significant_drop_tightening
- `packages/limit3r/src/bulkhead/tests.rs` — add significant_drop_tightening
