# Fix per-crate clippy.toml gaps

**Date:** 2026-03-19 19:52
**Task:** Create missing clippy.toml files for 4 workspace member crates

## Goal
All workspace member crates have a clippy.toml matching their layer/role.

## Approach
4 crates missing clippy.toml in `packages/`:
1. `packages/limit3r` - Pure library. Copy from existing `limit3r/clippy.toml` (library profile with global state bans). No Axum bans needed but harmless.
2. `packages/shedul3r-rs-sdk` - SDK library. Library profile with global state bans. Keep reqwest/serde bans (code uses `#[allow]` where needed).
3. `packages/pipelin3r` - Orchestration library. Library profile with global state bans. Same as SDK.
4. `packages/shedul3r-bin` - Stub binary. Service profile (no global state bans) since it's a composition root / entry point.

## Files to Create
- `packages/limit3r/clippy.toml` - copy from `limit3r/clippy.toml`
- `packages/shedul3r-rs-sdk/clippy.toml` - library profile, customized reasons
- `packages/pipelin3r/clippy.toml` - library profile, customized reasons
- `packages/shedul3r-bin/clippy.toml` - service profile (no global state bans), customized reasons
