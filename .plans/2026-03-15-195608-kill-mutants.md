# Kill 9 surviving mutants in SDK and pipelin3r

**Date:** 2026-03-15 19:56
**Task:** Kill remaining mutants: SDK client.rs line 304 (== to !=), client.rs line 365 (> to >=), pipelin3r agent.rs line 440 (7 mutations on partial failure check)

## Goal
All 9 mutants are killed by new tests that exercise the actual code paths.

## Approach

### SDK mutant 1: client.rs line 304 `== with !=`
- Existing test reconstructs logic inline — doesn't call `http_call`
- Add test using TCP mock (same pattern as bundle.rs `spawn_http_mock`) that returns `{"success": true, "output": "ok"}`
- Call `http_call` directly, verify `result.success == true`
- Also test `{"success": false}` returns `success == false`

### SDK mutant 2: client.rs line 365 `> with >=`
- Existing `mutant_kill_truncate_str_at_exact_boundary` should catch this
- The user says to verify and add if missing — it exists and looks correct
- May need a slightly different test that specifically detects `> vs >=` on line 365 (`while end > 0`)
- Actually the mutation at line 365 is `while end > 0` becoming `while end >= 0` — for usize this is always true but is_char_boundary(0) exits. Test: single multibyte char with max_len=1 should produce "" (backs up to 0)

### pipelin3r mutant: agent.rs line 440 (7 mutations)
- Existing test only runs all-success dry-run, then simulates other cases with local vars
- Need to test the actual code in agent.rs with different succeeded/failed combos
- Since we can't easily make dry-run tasks fail, extract the partial failure check into a helper function and test it directly
- Or: use tracing subscriber to capture the warn log and verify it fires/doesn't fire

## Files to Modify
- `packages/shedul3r-rs-sdk/src/client.rs` — add TCP mock test for http_call
- `packages/pipelin3r/src/agent.rs` — add tests for all 4 batch partial failure cases
