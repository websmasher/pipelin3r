# Fix shedul3r server security and correctness (Batch 2)

**Date:** 2026-03-15 17:13
**Task:** Fix 5 issues from adversarial review: API key auth, body limit mismatch, exit code bug, streaming downloads, dead sqlx dep.

## Goal
Address all Batch 2 findings from the adversarial review plan.

## Approach

### 1. API key auth middleware
- Create `auth.rs` in api/src/ with Axum middleware function
- Read SHEDUL3R_API_KEY env var in main.rs (allowed there per architecture rules)
- Conditionally apply middleware layer
- Return 401 JSON on failure

### 2. Body limit fix
- Remove global 50MB DefaultBodyLimit
- Add per-route: 50MB on task routes, 100MB on bundle upload, default on others

### 3. Exit code fix
- In `build_task_response`, change hardcoded `exit_code: 1` to `result.exit_code`

### 4. Stream bundle downloads
- Add tokio-util dep to workspace and api crate
- Replace `tokio::fs::read` with `File::open` + `ReaderStream` + `Body::from_stream`

### 5. Remove dead sqlx
- Remove from workspace dependencies in apps/shedul3r/Cargo.toml

## Files to Modify
- `apps/shedul3r/crates/adapters/inbound/api/src/auth.rs` — NEW: auth middleware
- `apps/shedul3r/crates/adapters/inbound/api/src/lib.rs` — add auth module
- `apps/shedul3r/crates/adapters/inbound/api/src/main.rs` — wire auth, fix body limits
- `apps/shedul3r/crates/adapters/inbound/api/Cargo.toml` — add tokio-util dep
- `apps/shedul3r/crates/app/commands/src/execute.rs` — fix exit code
- `apps/shedul3r/crates/adapters/inbound/api/src/handlers/bundles.rs` — streaming download
- `apps/shedul3r/Cargo.toml` — remove sqlx, add tokio-util
