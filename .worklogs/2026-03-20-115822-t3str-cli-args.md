# Fix t3str CLI arg names to match pipeline

## Summary

Renamed CLI args from `--repo`/`--lang` to `--repo-dir`/`--language` to match what the pipeline sends. Bumped version to 0.1.1.

## Key Files

- `apps/t3str/crates/adapters/inbound/cli/src/main.rs` — arg name changes
- `apps/t3str/Cargo.toml` — version bump to 0.1.1
