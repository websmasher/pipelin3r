# Transform builder + integration tests

**Date:** 2026-03-15 13:20
**Scope:** pipelin3r transform, integration tests

## Summary
Implemented TransformBuilder (read files → apply Rust function → write output). Added 4 integration tests covering full agent dry-run flow (single + batch) and transform end-to-end. 54 unit + 4 integration tests for pipelin3r.
