# Fix all adversarial review findings

**Date:** 2026-03-15 13:58
**Scope:** All four packages — 5 critical, 9 high fixes

## Summary
Fixed all critical and high findings from adversarial review:
- shedul3r: path traversal (absolute paths), body size limits, file count limits
- limit3r: config validation, memory eviction (max 10K keys), retry error preservation, duration_serde safety, doc fixes
- SDK: poll timeout (max_poll_duration), anyhow→thiserror (SdkError enum)
- pipelin3r: Auth::FromEnv error on missing vars, template single-pass (no cross-injection), remote bundle cleanup, tokio explicit features, anyhow→thiserror (PipelineError enum), HashMap→BTreeMap, bundle tempdir uniqueness

134 unit/integration tests + 31 golden tests, all passing.
