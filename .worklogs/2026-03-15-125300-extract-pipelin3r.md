# Extract pipelin3r: pipeline orchestration package

**Date:** 2026-03-15 12:53
**Scope:** packages/pipelin3r — 8 modules, 34 tests

## Summary
Extracted pipeline orchestration from websmasher dev-process into standalone pipelin3r package. Auth injection, template filler, executor with dry-run, agent builder (single/batch), bundle, command, pool, task YAML. 34 tests.

## Key Files for Context
- packages/pipelin3r/src/executor.rs — wraps SDK client + auth + dry-run
- packages/pipelin3r/src/agent.rs — builder pattern for LLM invocations
- packages/pipelin3r/src/auth.rs — per-invocation auth (OAuth, API key, env, custom)
- packages/pipelin3r/src/template.rs — two-phase injection-safe template filler
