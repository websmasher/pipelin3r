# Fix all adversarial review v2 findings

**Date:** 2026-03-15 17:22
**Scope:** All four crates — massive fix batch

## Summary
180 tests + 31 golden, all passing. Major fixes across all crates.

### limit3r
- Circuit breaker min call threshold (no premature trip on partial window)
- >= threshold comparison
- 28 new tests (circuit breaker, config validation, duration serde)

### shedul3r
- ELIMINATED CODE DUPLICATION: adapter crate now re-exports from limit3r instead of reimplementing
- API key auth middleware (SHEDUL3R_API_KEY env var, optional)
- Per-route body limits (50MB tasks, 100MB bundles)
- Exit code preserved (was hardcoded to 1)
- Streaming bundle downloads (was reading entire file to memory)
- Removed dead sqlx dependency

### SDK
- http_call returns real Err on network failures (was always Ok)
- URL-encoded bundle paths
- TaskResult includes metadata (exit_code, elapsed, started_at)
- Added limiter_key and timeout_ms to TaskPayload
- require_success() method on TaskResult
- File-poll recovery tested
- 7 new tests

### pipelin3r
- Bundle path traversal validation
- Template ALL phases single-pass (no cross-injection anywhere)
- Tool enum (typed, not strings)
- Dry-run captures auth env (redacted) + bundle file list
- PipelineError: AgentFailed, BatchPartialFailure, Timeout variants
- TemplateFiller owned self for chaining + from_file()
- executor.command() and executor.transform() convenience methods
- 11 new tests
